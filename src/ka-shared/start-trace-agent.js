// @flow
/**
 * This is the public facing, export for starting trace agent. Allows us to
 * easily test the function itself separately.
 */
import {startTraceAgent} from "./start-trace-agent-impl.js";

startTraceAgent();
