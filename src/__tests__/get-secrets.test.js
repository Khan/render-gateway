// @flow

describe("#getSecrets", () => {
    describe("in production", () => {
        it.todo("should lookup secrets with render-gateway crypto key path");
    });

    describe("not in production", () => {
        it.todo("should throw");
    });
});
